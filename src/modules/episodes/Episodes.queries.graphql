query GetEpisodes($page: Int) {
  episodes(page: $page) {
    results {
      air_date
      episode
      id
      name
      characters {
        id
        name
      }
    }
    info {
      count
      pages
      next
      prev
    }
  }
}
